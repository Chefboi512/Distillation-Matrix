# MVSEP API key — required by the dev proxy (proxy-server.js).
# Get yours at https://mvsep.com/
#
# For Cloudflare Pages deploy, set this in:
#   Dashboard → Pages → your-site → Settings → Environment variables
# (NOT in a .env file committed to the repo.)
MVSEP_API_KEY=your_key_here
